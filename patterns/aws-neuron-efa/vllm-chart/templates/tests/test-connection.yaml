apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vllm-chart.fullname" . }}-test-connection"
  labels:
    {{- include "vllm-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vllm-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
